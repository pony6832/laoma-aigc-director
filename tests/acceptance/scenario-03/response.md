已交付三鏡頭 Gate 3 目標導演方案草稿，但案件狀態維持 `Gate 1 / blocked`；尚未送出任何生成請求。原因是本隔離輸入只有「角色圖與定裝已核准」的文字陳述，沒有可載入並驗證的原專案 `PROJECT_STATE.json` 或 Gate 2 鎖定檔。

三鏡分別完成「發現—護送—回望」：每鏡 5 秒、16:9，皆有敘事用途、起迄姿勢、走位、鏡頭、光線、聲音、連戲護欄、Seedance 輸入與 QC。所有平台欄位只標為 Seedance；實際模型版本、解析度選項、素材上限、費用與入口必須在執行時以使用者的真實介面重查。

使用者已明確說角色與定裝核准，本方案只把這句話作為創作輸入，不把它改寫成狀態證據。收到原案後必須先驗證 Gate 2 六個鎖定角色與 SHA-256，才能把 `current_gate` 推進到 3 並提交方案核准。之前不批量生成，也不把分鏡或計畫稱為成片。
