目前維持 `Gate 1 / blocked`，代碼 `tool_unavailable`。本驗收沒有可驗證的原專案狀態，只提供工具失敗這個條件，且沒有實際生成器或錯誤日誌可呼叫／重現；因此我不猜實際 current Gate，也沒有偽造命令輸出、job ID、截圖或影片檔。

我已建立最小 5 秒重試規格與 checkpoint：先保留 Gate 3 鎖定版本，記錄真實工具／模型／環境錯誤，再固定角色、場景、單一動作與固定鏡位重試；每次只改一個主變數。實際輸出存在並通過單鏡 QC 前不移向 Gate 4。

若原工具無法恢復，可以提出替代入口，但必須由使用者核准並重新核對平台專屬參數；本次沒有切換入口。
